// This is the development environment configuration for the application. 
// It contains settings that are specific to the development environment.
export const environment = {
  production: false,
  apiUrl: 'https://localhost:7092/api'
};