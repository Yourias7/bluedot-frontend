// This is the production environment configuration for the application. 
// It contains settings that are specific to the production environment.
export const environment = {
  production: false,
  apiUrl: 'https://localhost:7092/api'
};